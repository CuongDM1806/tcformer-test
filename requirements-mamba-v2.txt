# Install requirements.txt and CUDA-enabled PyTorch first, then install this
# file with --no-build-isolation so mamba-ssm can select the matching wheel.
# mamba-ssm 2.2.6 imports generation aliases removed by Transformers 5.x.
transformers==4.44.2
# Mamba-2's fused SSD path calls this CUDA extension directly. It is not an
# active dependency of mamba-ssm 2.2.6, so install the matching Torch 2.7 wheel.
causal-conv1d==1.5.4
mamba-ssm==2.2.6.post3
